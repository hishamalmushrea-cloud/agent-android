# Task Center

The home screen now observes the Room task flow and displays scheduled tasks with their status. Scheduled tasks can be cancelled from the UI; cancellation updates WorkManager and persists `CANCELLED` in Room. This is the first usable task-center slice. Editing, retry, filtering, and history separation remain planned after device build verification.
